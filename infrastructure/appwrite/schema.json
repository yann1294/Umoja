{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Umoja Appwrite infrastructure",
  "type": "object",
  "required": ["project", "team", "database", "storage", "buckets"],
  "properties": {
    "project": { "type": "object", "required": ["name", "platforms"] },
    "team": { "type": "object", "required": ["id", "name", "roles"] },
    "database": { "type": "object", "required": ["id", "name", "tables"] },
    "storage": {
      "type": "object",
      "required": ["sharedFreePlanBucket", "cmsMedia", "intakeFiles"]
    },
    "buckets": { "type": "array", "minItems": 1 }
  },
  "additionalProperties": false
}
